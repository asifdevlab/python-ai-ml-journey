# 📘 Python Fundamentals – Assignment 1

## Questions

### Q1
Write a program that asks the user for their **name** and **age**, then prints a sentence like:  
```
Hello Shradha, you are 21 years old!

---

### Q2
Take two numbers as input from the user and print their **product** and **quotient**.

---

### Q3
Ask the user to enter **two integers** and **one float**.  
Convert them all to floats and print their **average**.

---

### Q4
The user enters a string containing a number (e.g., `"45"`). Convert it to:
- An integer  
- A float  
- A string again  

Print all three values with their types.

---

### Q5
Evaluate and print the result of the following expression:
```python
x = 10 + 3 * 2 ** 2
```
Explain why the output is what it is based on operator precedence.

---

### Q6
Write a program to **swap values of two numbers** entered by the user.

---

### Q7
Ask the user for a **temperature in Celsius** (string input).  
Convert it to float, then calculate and print temperature in **Fahrenheit** using the formula:  
```
FahrenheitTemp = (CelsiusTemp * 9/5) + 32
```

---

### Q8
Take the **radius (r)** as user input and print the **area of a circle** using the formula:  
```
Area = 3.14 * r^2
```

---

### Q9
Ask the user for **Principal (P)**, **Rate (R)**, and **Time (T)**.  
Convert all to float and compute **Simple Interest (SI)**:  
```
SI = (P * R * T) / 100
```

---

### Q10
Take a **decimal number** as input (like `45.78`) and output its:
- Integer part → `45`  
- Fractional part → `.78`
```

---
